<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/output.css" />
    <title>View all Users</title>
  </head>
  <body>
    <div class="min-h-screen w-full box-border bg-gray-950 pt-8">
      <a href="/home" class="text-blue-500 ml-[50px]">Back to Home </a>
      <div class="mt-7 ml-8 mr-8 flex gap-8 flex-wrap justify-center">
        <% if(allUsers.length>0) { %> <% allUsers.map((data)=>{ %>
        <div
          class="bg-[#171717] w-[360px] h-[340px] rounded-t-[24px] rounded-b-xl p-3 box-border overflow-hidden"
        >
          <div
            class="w-full h-46 bg-white rounded-xl mb-[4px] overflow-hidden shadow-[0px_01px_35px_1px_rgba(255,255,255,0.3)]"
          >
            <img
              src="<%= data.imgurl %>"
              alt="img of <%= data.name%>"
              class="object-cover object-top rounded-xl"
            />
          </div>
          <h1 class="text-gray-100 select-none mt-5 font-bold text-xl">
            <%=data.name%>
          </h1>
          <p class="text-gray-400 select-none"><%=data.email%></p>
          <div class="mt-4 ml-auto w-fit text-center">
            <a href="/edit/<%= data._id %>" class="text-blue-500">Edit</a>
            <a href="/delete/<%= data._id %>" class="text-red-500 ml-1"
              >Delete</a
            >
          </div>
        </div>
        <% }) %> <% }else { %>
        <h1 class="text-md text-zinc-500 px-6 pt-6">No Users Found</h1>
        <% } %>
      </div>
    </div>
  </body>
</html>
